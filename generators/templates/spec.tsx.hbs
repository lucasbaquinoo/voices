import { renderWithTheme } from 'lib/contexts/tests';
import { {{pascalCase name}} } from './{{pascalCase name}}';

describe('<{{pascalCase name}} />', () => {
  it('should render the {{pascalCase name}}', () => {
    const { container } = renderWithTheme(<{{pascalCase name}} />);
  });
});
